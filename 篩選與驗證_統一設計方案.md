# 篩選 × 驗證 統一設計方案（審閱版）

> **狀態**：審閱定案。以下 §9「定案修訂」為最終規格，與前文衝突處以 §9 為準。
> **Phase 1（DB 層）已實作完成**（見 §10）。


> 目的：把「機會雷達（盤中七因子）」與「觀察名單（隔夜抗跌）」兩套互不相通的
> 系統打通，讓盤中挑到的標的能進入隔日名單、並用「對齊選股理由」的規則做驗證，
> 每日逐檔留痕（含落選理由），供持續優化。
> 原則：**後端定案、零前端 filter**（延續既有 canonical 決策）。

---

## 0. 現況問題（為什麼要改）

| 問題 | 根因（程式位置） |
|---|---|
| 盤中「吻合」的檔沒進盤後驗證 | 盤後驗證頁上方是**系統 A（隔夜抗跌清單）**，盤中吻合的檔在**系統 B（可操作/觀察）**，兩者名單不同 |
| 觀察清單命中率 6 天全 0% | `after_hours.verify_today()` 命中定義＝「漲>2%且量比>1.5」，但選股理由是「抗跌」→ 結構性矛盾 |
| 可操作命中 0/10 | `review_rules` 用**同日**收盤漲≥1.5% 判可操作；七因子本質是「隔日續強」，同日門檻過嚴 |
| 「累積 1 交易日」看不到歷史 | 資料其實按 `trade_date` 存、不會被刷掉；但 UI 把所有天加總成一個數字、無日期選擇器 |
| 無法優化 | 只存**選入**理由（`watchlist.reason`），落選股直接 `continue`、不留痕 |

現有兩套（都是 live，經 `server.py` 掛載 `vps_intraday_test.router`）：

- **系統 A**：`/api/review` → `db.py`（`mls.db`：`watchlist` / `review_log`）
- **系統 B**：`/api/review/rules` → `review_rules.py`（`intraday_eod.db`：`rule_signals`）

---

## 1. 目標架構

```
盤中每輪  機會雷達(七因子) ─┬─ 可操作 / 觀察 / 排除
  /api/intraday-test        │        │
                            │        └─record()→ rule_signals(同日快照，保留)
                            │
   收盤 15:05 after_hours ──┤ 取「今日可操作 + 觀察(收盤>訊號價)」存活者
                            │        ∪ 隔夜抗跌篩選(保留為次要來源)
                            │        去重 → 明日觀察名單(watchlist)
                            │        同時：落選池(watch_reject) 逐檔記卡在哪個因子
                            ▼
   隔日 08:55 reverify ─────┤ 開盤重驗(跳空破前低→降級)  ← 已有 mark_reverify
                            ▼
   隔日收盤 T+1 驗證 ───────┤ 對齊選股理由判定命中：
                            │   ‧ 抗跌來源 → 隔日相對族群強 / 收紅
                            │   ‧ 可操作來源 → 隔日延續(收盤>進場基準)
                            ▼
                     watch_outcome(逐檔判定) + review_log(逐日命中率)
```

核心變更：**明日名單的主來源從「純抗跌」改為「今日機會雷達存活者」**，抗跌降為補充；
驗證從「同日、單一門檻」改為「T+1、依來源分流」。

---

## 2. Schema 變更（`mls.db`）

### 2.1 `watchlist`（擴充，不破壞現有欄位）

```sql
ALTER TABLE watchlist ADD COLUMN source       TEXT;   -- 'radar' | 'resilient'(抗跌) | 'both'
ALTER TABLE watchlist ADD COLUMN entry_ref    REAL;   -- 進場基準價：收盤價(可操作)或訊號價
ALTER TABLE watchlist ADD COLUMN factor_score REAL;   -- 七因子分數(radar 來源)
ALTER TABLE watchlist ADD COLUMN group_at_pick TEXT;  -- 選入當下分類(可操作/觀察)
-- 既有：trade_date, stock_id, stock_name, sector, reason, reverified, demoted, hit
```

### 2.2 `watch_reject`（新增：落選池，供優化）

```sql
CREATE TABLE IF NOT EXISTS watch_reject(
  trade_date  TEXT,          -- 供隔日使用的名單日
  stock_id    TEXT,
  stock_name  TEXT,
  sector      TEXT,
  source      TEXT,          -- radar / resilient
  factor_score REAL,
  fail_factor TEXT,          -- 卡在哪：'量比<0.8' / '法人買超<=0' / '七因子<65' / '極端價NO_DATA' ...
  detail      TEXT,          -- 該因子的實際值，例如 '量比0.6'
  PRIMARY KEY(trade_date, stock_id)
);
```

> `build_tomorrow_watchlist()` 內每個 `continue` 之前改成「記一筆 watch_reject」。

### 2.3 `watch_outcome`（已存在，補用 T+1）

現有欄位已夠：`open_group / close_group / verdict / change_rate / aflow / volume_ratio`。
新增語意：`verdict` 依 `source` 分流判定（見 §3）。**判定日 = T+1 收盤**，不是選入日。

### 2.4 `rule_signals`（系統 B，保留同日快照，加 T+1 欄）

```sql
ALTER TABLE rule_signals ADD COLUMN next_close  REAL;   -- T+1 收盤
ALTER TABLE rule_signals ADD COLUMN next_result TEXT;   -- T+1 判定
```

> 同日 `result` 保留（觀測用），但**可操作對外命中率改看 `next_result`**。

---

## 3. 驗證規則（對齊選股理由）

| 來源 source | 選入理由 | 命中定義（T+1 收盤 vs 基準） | 基準 entry_ref |
|---|---|---|---|
| `radar`（可操作） | 七因子≥65，明天續強 | 隔日收盤 ≥ 基準 × (1+0%) 且相對族群為正 | 選入日收盤價 |
| `radar`（觀察） | 收盤>訊號價，突破確認 | 隔日收盤 > 基準 | 選入日訊號價 |
| `resilient`（抗跌） | 資金流出族群逆勢抗跌 | 隔日「個股漲幅 − 族群中位」> 0（相對強） | — |

門檻集中一處（沿用 `review_rules.RULES` 風格），要調靈敏度只改常數。

`review_log.hit_rate` = 當日名單 T+1 命中數 / 名單總數。
`watch_reject` 不算入命中率，但可回頭統計「落選者若當初選入的假設命中率」→ 檢驗門檻鬆緊。

---

## 4. 排程與資料流（`after_hours.run()` 改動）

1. **15:05 收盤**：
   - 讀今日 `rule_signals`（同日 buffer）取「可操作 + 觀察存活者」。
   - `build_tomorrow_watchlist()`：`radar_survivors ∪ resilient`，去重（radar 優先），上限 10。
   - 落選者寫 `watch_reject`。
   - `save_watchlist(tomorrow, wl)` 帶新欄位（source/entry_ref/...）。
2. **T+1 08:55**：`reverify_watchlist()`（已有）標記跳空破前低降級。
3. **T+1 15:05**：新增 `verify_prev_watchlist()`——用昨日名單 + 今日收盤，依 §3 判 `watch_outcome`、寫 `review_log`、回填 `rule_signals.next_result`。

> 注意：現在的 `verify_today()` 是「同日驗證今日名單」，改為「T+1 驗證昨日名單」。
> 這也修好「6 天全 0%」——因為判定終於對齊了選股理由。

---

## 5. UI 重構（盤後驗證頁）

現況：頁面預設 `db.today()`，週末/隔日打開全空、無法翻歷史。

```
┌ 盤後驗證 ───────────────────────────────── [返回] ┐
│ [◀] 交易日: 2026-07-24 ▼   ← 日期選擇器(下拉/日曆)  │  ← 新增，預設「最近有資料日」
│ Tabs:  ● 觀察名單驗證   ○ 分類規則(七因子)   ○ 落選複盤 │  ← 三分頁
├───────────────────────────────────────────────┤
│ [觀察名單驗證]                                       │
│  今日名單 N 檔 · T+1 命中 X · 命中率 XX%             │
│  逐檔表：代碼 名稱 來源 進場基準 T+1收盤 相對族群 判定  │
│  逐日趨勢：日期 命中率(可點回看)  ← 取代單一加總數字   │
├───────────────────────────────────────────────┤
│ [分類規則] 可操作/觀察/排除 × (同日 vs 隔日) 兩欄命中率 │
├───────────────────────────────────────────────┤
│ [落選複盤] 卡在哪個因子分布圖 + 逐檔落選理由           │
│           「若選入的假設命中率」← 判斷門檻鬆緊         │
└───────────────────────────────────────────────┘
```

- 預設日期 = 後端回「最近一個有驗證資料的交易日」（非 `today()`），週末打開就自動落在 07-24。
- 日期來源：`SELECT DISTINCT trade_date FROM watch_outcome ORDER BY DESC`。
- 這一頁全部只讀既有 API + 新增 `/api/review/stocks?trade_date=` 已存在，可直接接。

---

## 6. 需要新增/修改的 API

| 端點 | 動作 |
|---|---|
| `/api/review/dates` | **新增**：回有驗證資料的交易日清單（給日期選擇器） |
| `/api/review` | 加 `trade_date` 參數；預設回「最近有資料日」而非 today |
| `/api/review/stocks` | 已存在，補回 `source / entry_ref / next_close / verdict` 欄位 |
| `/api/review/rejects?trade_date=` | **新增**：落選池 |
| `/api/review/rules` | 加隔日欄（同日/隔日兩欄命中率） |

---

## 7. 遷移與風險

- 全部用 `ALTER TABLE ... ADD COLUMN`（SQLite 相容既有 `db.init()` 遷移寫法），舊資料不動、新欄位 NULL。
- 舊 `review_log` 歷史 0% 會保留（是真實紀錄）；新規則從改版日起生效，命中率會分段。
- `verify_today` → `verify_prev_watchlist` 換法時，需一天空窗（改版當日既非舊也非新），建議收盤後部署。
- 系統 B 同日 `result` 不刪，保留為觀測值，避免破壞既有累積。

---

## 8. 建議施工順序

1. **schema + 落選池**（低風險，先讓資料開始留痕，越早越好）。
2. **驗證規則對齊 + T+1 驗證**（修 0% 的根本）。
3. **明日名單合流（雷達→名單）**（改變名單來源，影響最大，最後上）。
4. **UI 日期選擇器 + 三分頁**（可與 1–3 並行，純前端＋唯讀 API）。

> 每一塊都可獨立部署、獨立回滾。建議 1 與 4 先上（不改決策邏輯、只加觀測），
> 累積幾天資料後再上 2、3。

---

## 9. 定案修訂（審閱後，最終規格）

**A. `after_hours.run()` 相容性（必要修正）**
補驗昨日與產出明日**同一輪都要做**，不是二選一；且用「前一交易日」，不是自然 yesterday。

```python
def run():
    trade_date = db.latest_trade_date()
    prev = db.previous_trade_date(trade_date)   # 前一交易日，非自然日
    if db.watchlist_exists(prev) and not db.is_watchlist_verified(prev):
        verify_prev_watchlist(prev, trade_date)   # 1. 先補驗昨日
    build_tomorrow_watchlist(trade_date)          # 2. 一定產出明日
```
（Phase 3 才切這段；需新增 `latest_trade_date / previous_trade_date /
watchlist_exists / is_watchlist_verified` 交易日行事曆輔助函式。）

**B. 可操作（Radar）命中改 A/B/C 三級**（取代二元 Hit/Miss，全部要求相對族群>0）

| 級別 | 條件（T+1 vs entry_ref） | 意義 |
|---|---|---|
| A Success | ≥ `RADAR_T1_SUCCESS`(+2%) 且 relative>0 | 真正成功（有肉） |
| B Continue | `RADAR_T1_CONTINUE_MIN`(+0.5%)~2% 且 relative>0 | 續強 |
| C Fail | <0.5% 或 relative<0 | 失敗 |

Dashboard 同時顯示「真實命中率=A/Total」與「含續強=(A+B)/Total」；兩者差距大
→ 抓得到方向、抓不到爆發力 → 下一步調高量比/籌碼權重。門檻常數已放 `config.py`。

**C. 觀察（Radar）改看 T+1 最高價，四狀態**（收盤會吃掉早盤突破，不合理）
未突破（高≤訊號價）／突破失敗（高>訊號價但收<訊號價）／突破站穩（收≥訊號價）／
突破延續（收≥訊號價×1.02）。需 `rule_signals` 或 stock_daily 有 `high` 欄。

**D. 抗跌（Resilient）加絕對跌幅下限**：`relative>0 AND (跌幅>-2% 或 收紅)`。
常數 `RESILIENT_T1_FLOOR=-0.02`。避免「族群-8%、個股-6%、相對+2%」這種還是重傷的單。

**E. 名單來源 = Radar 優先、Resilient 補足（不 union）**
Radar 存活者依分數排序取前 10；不足 10 檔才用 Resilient（相對強度最高者、去重）補足。
`source` 欄如實標記 `radar`/`resilient`，驗證分流才知道套哪套規則。

**F. `watch_reject` 存全因子分數**：score_trend/volume/chip/sector/rs/ai/total，
不只 fail_factor/detail。PK 含 `source`（同檔可雙流程各落選一次）。
→ 未來 `SELECT fail_factor, AVG(score_total) GROUP BY fail_factor` 即知門檻鬆緊。

**G. `review_log` 存報酬分布**：avg/median/max/min return（+ avg_holding_days 預留）。
report 統一用 `(T+1收盤/entry_ref)-1`，不混族群相對報酬。→ 分辨「低勝率高報酬」vs「高勝率沒肉」。

**H. Model Version**：`config.MODEL_VERSION`（主.次：主=名單來源變、次=門檻/權重變），
蓋在 watchlist/review_log/watch_reject；Dashboard 折線圖分版比較。

**I. 遷移**：一律 `PRAGMA table_info()` 檢查後才 ADD COLUMN（SQLite 無可靠
IF NOT EXISTS，重啟重跑 init 會報 duplicate column）。

**J. Phase 5（AI 優化）**：只「發出建議 + 模擬回測報表」，人工點套用才改 RULES；
先離線 Jupyter 跑、累積 20~30 交易日再開始，避免過擬合。

**五階段上線**：
1. Schema 擴充 + watch_reject + review_log 擴充 + version 欄位 ← **本批**
2. UI 日期選擇器 + 歷史回看 + 趨勢圖 + 落選分析（不改模型）
3. T+1 驗證 + Radar/Resilient 分流 + A/B/C 規則
4. Radar 優先、Resilient 補足名單生成
5. AI 自動優化（建議制）

---

## 10. Phase 1 實作紀錄（已完成）

改動檔案（皆為加法、零決策邏輯變更；本機以 mls.db 副本驗證遷移冪等）：

- **config.py**：新增 `MODEL_VERSION="v4.1"`、`RADAR_T1_SUCCESS/CONTINUE_MIN`、`RESILIENT_T1_FLOOR`。
- **db.py**：
  - `_column_exists()/_add_column()`（PRAGMA 冪等遷移）。
  - `watchlist` +source/entry_ref/factor_score/group_at_pick/model_version。
  - `review_log` +avg/median/max/min_return/avg_holding_days/model_version。
  - 新表 `watch_reject`（PK 含 source，含全因子分數欄）。
  - `save_watchlist()` 擴充寫新欄；新增 `save_watch_rejects()/load_watch_rejects()`；
    `write_review(...,stats=)` 具名欄位寫入報酬統計；`review_dates()/latest_review_date()`。
- **review_rules.py**：`rule_signals` +next_close/next_result/next_relative_sector/verified_at（PRAGMA 冪等）。
- **after_hours.py**：`build_tomorrow_watchlist()` 回 `(rows, rejects)`，落選逐檔留痕
  （source=resilient，帶 fail_factor/detail；七因子分數留 NULL 待 radar 路徑回填）；
  `run()` 存 watch_reject。**名單選股結果不變**，僅開始留痕。

驗證：遷移連跑兩次不報錯；新欄位寫讀正常；review_rules judge/record/validate/api_payload 通過。
**未動**：驗證判定規則、名單來源、任何 UI/API 對外行為 —— 全部留待 Phase 2+。

---

## 11. Phase 3+4 實作紀錄（已完成，模型層）

依「週一開盤即新模型」需求，Phase 3（T+1 分流驗證）與 Phase 4（Radar 優先名單）
一併實作；UI（Phase 2）延後。全部集中在 `after_hours.py`（18:00 排程），且**每條新
路徑都帶 try/except 回退舊行為，任何失敗最差回到「純抗跌名單＋舊二元驗證」，不會
使名單變空、不會讓排程 throw**。

- **純函式（可單測）**：
  - `judge_watchlist_row(source, close_change, relative)`：radar→A/B/C（真實命中＝A、
    含續強＝A+B）、resilient→相對族群為正且跌幅未破 -2%、舊格式→回 None 走相容判定。
    對齊點：`change_rate` 基準＝前一交易日收盤＝選股日收盤（entry_ref），故它直接就是
    相對進場報酬，T+1 續強判定天然成立。
  - `select_radar_watchlist(radar_rows, resilient_rows, limit=10)`：可操作(分數)→觀察→
    抗跌補足，去重；回 picks + radar 落選（帶因子分數）。
  - `_radar_snapshot_rows(date)`：讀 `intraday_live_snapshot.json`，日期不符/讀不到回 []。
- **`verify_today(snaps, sectors, sig, strong)`**：改 T+1 分流；彙總報酬分布寫 review_log；
  逐檔 verdict 寫 watch_outcome。headline 命中率＝真實命中(A)；A/B/C 明細在 watch_outcome。
- **`run()`**：驗證包 try（失敗回退舊二元判定）；名單改 Radar 優先＋Resilient 補足
  （radar 空→回退純抗跌）；落選＝radar 落選＋抗跌未過濾＋抗跌過關但名額被 radar 佔走。
- **未動**：`review_rules`（系統 B 同日快照照舊累積，next_result 欄位留 Phase later 回填）；
  交易日曆輔助函式（run() 本就同輪驗今日＋產明日，暫不需 previous_trade_date）。

測試：`tests/test_after_hours_model.py`（judge 分流 / select 優先補足落選 /
verify_today 整合）全過；`tests/test_review_rules.py` 不受影響。

**Monday 過渡日行為**：週五用舊邏輯產的「週一名單」source=NULL → 週一 18:00 走相容
判定驗證（不 crash）；週一 18:00 產出的「週二名單」為第一份 Radar 優先新名單。

---

## 12. Phase 2 實作紀錄（已完成，UI + 唯讀 API）

盤後驗證頁重構為「日期選擇器 + 三分頁」，全部唯讀、零決策邏輯：

- **後端（皆唯讀）**：
  - `/api/review?trade_date=`：預設 `latest_review_date()`（非 today，週末/隔日開啟不再全空），回該日 summary（含報酬分布）+ 逐檔 outcomes（watch_outcome 併 watchlist 的 source/entry_ref）+ dates + 逐日趨勢。
  - `/api/review/dates`：交易日清單（日期選擇器）。
  - `/api/review/rejects?trade_date=`：落選池。
  - `db.recent_hit_rates` 擴充回報酬欄；新增 `review_summary/review_outcomes`。
    watch_outcome 尚無資料的舊日 → 退回 watchlist 逐檔標「待驗證」。
- **前端（intraday_decision.html）**：`openReview()` 取代舊面板 →
  日期下拉 + 三分頁：
  · 觀察名單驗證：名單檔數／真實命中A／含續強A+B／報酬中位 四卡 + 逐檔
    T+1 表（來源/進場基準/T+1收盤/相對族群/判定）+ 逐日趨勢（點日期回看）。
  · 分類規則命中率：沿用（渲染進 #rvBody）。
  · 落選複盤：卡在哪個因子分布 + 逐檔明細。
  判定 badge 沿用既有 .vd（紅=命中、綠=反向），新增 .v-cont（amber=續強B）。

驗證：已部署 live，瀏覽器實測三分頁切換正常、日期選擇器帶 6 日、預設落
2026-07-24、空狀態訊息正確、無 console 錯誤；分類規則分頁顯示今日(07-27)
盤中即時訊號累積中。

**設計文件全五階段狀態**：Phase 1/2/3/4 已完成上線；Phase 5（AI 建議制）
與兩個精緻化（§9-C 觀察四狀態、§9-F 逐因子落選分數）仍為待辦。

---

## 13. Phase 5 + 兩精緻化 實作紀錄（已完成）

**§9-C 觀察四狀態**（after_hours.judge_watchlist_row）
radar「觀察」來源且有 T+1 最高價時，改判四狀態：突破延續(收盤續強≥+2%且相對>0
=真實命中)／突破站穩(收盤守住進場基準)／突破失敗(盤中拉過但收盤被殺回)／未突破
(沒人拉)。缺 high 自動退回 A/B/C。verify_today 傳 group_at_pick/high/entry_ref。
前端 badge：突破延續=紅(命中)、突破站穩=amber(續強)、突破失敗/未突破=綠。

**§9-F 逐因子落選分數**（watch_reject +factors_json）
radar 七因子 taxonomy(money_health/absorption/net_active/vs_ma20/inst_streak/
margin)與通用欄非一一對應：只把「真正對得上」的填具名欄
（volume=net_active、rs=vs_ma20、chip=inst_streak），完整 points 存 factors_json
（誠實，不硬塞）。`_reject_factor_scores()` 攤平；落選複盤 UI 加主動/均線/法人欄。

**Phase 5 離線建議工具**（repo 根 `模型優化建議.py`）
唯讀跑 review_log/watch_outcome/watch_reject/rule_signals，產出：資料涵蓋、
來源分流命中率(A vs A+B)、報酬分布、**落選因子門檻鬆緊診斷**(某因子刷掉多數股
但平均總分偏高→建議放寬)、分類規則命中率、優化建議彙總。**只出建議、不改任何
RULES**；交易日數 < MIN_DAYS(預設15) 只列現況不下結論(防過擬合)。要採用須人工
改 config.py 常數、重新部署、用新 model_version 分版比較。

已部署 live（2026-07-29）；VPS 實跑：watch_outcome 已累積 07-28~07-29 兩日，
Phase 5 正確顯示「資料不足、還需約 13 個交易日」。tests/test_after_hours_model.py
新增 test_observe_four_state / test_reject_factor_scores 全過。

**設計文件全五階段：Phase 1~5 全部完成上線。** 後續為資料累積後的實際調參
（人工、依 Phase 5 建議），不再有待開發的架構項。
