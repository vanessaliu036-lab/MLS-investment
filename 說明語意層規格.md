# MLS 說明語意層規格（Explain Contract）

版本：v1.0
日期：2026-08-07
適用系統：MLS 台股盤中決策與盤後籌碼篩選
定位：**把「說明文字」從顯示層抽出來，改由後台單一來源產生；前台只負責印字。**

---

## 0. 一句話目標

> 後台收集數據、做專業分析、產出「結構化事實＋一句制式白話」；前台只把這句話印出來，不再自己用分數現算語意。

改說明文字 = 只改後台這一份對照表；**永遠不會動到篩選邏輯，也不會動到表格版面。**

---

## 1. 問題定義（現況病灶）

目前的淘汰／解讀說明**不是後台算好給前端顯示的，而是前端拿一個壓扁的分數在畫面上現編字串**，而且有三份各自為政、用詞不一致的版本：

| # | 位置 | 依據 | 產出用詞（同一情境） |
|---|---|---|---|
| 1 | `intraday_decision_dataflow.html:722` `rejectExplain()` | `score_total` 分段（<40 / <65） | 「**動能有**、基本面偏弱 → 短打不留倉」 |
| 2 | `intraday_decision_dataflow.html:1099` `plain()` | `score_pct` 分段（<40 / <65） | 「**有動能**、基本面偏弱 → 短打不留倉」 |
| 3 | `app/ai_explain.py:34` `local_explain()`（後端） | 象限 quadrant + 力道 | 「真攻擊」「強惜售」「假紅」 |

### 1.1 鐵證

`#1` 與 `#2` 表達同一件事，卻：

- 一個寫「動能有」、一個寫「有動能」（詞序不同）
- 一個句尾有句號、一個沒有
- 依據還不同（一個吃 `score_total`、一個吃 `score_pct`）

這就是使用者回報的「**相同篩選卻出現不一樣的說法**」。

### 1.2 架構性錯誤

語意（這檔到底怎麼回事）是在**顯示層**用單一分數**重新推導**的。因此：

- 「改文字」與「改判斷」被焊死：在現在的程式裡，文字**就是**邏輯。
- 訴求「改說明不該動到後台篩選」在現況**做不到**，正因兩者綁在一起。

---

## 2. 核心設計原則

```
後台（專業）                              前台（白話）
──────────                               ──────────
收集數據 → 分析 → 產出「結構化事實 + 一句制式白話」
   │                                          │
   ├ 盤中續強?   group_strong                 └ 直接印出後台給的 row.explain
   ├ 資金流入?   flow / flow_ratio                （不再有任何 if score<40 現算）
   ├ 法人買賣超  inst_net
   ├ 連續流入    flow_streak
   ├ 支撐        support_state
   ├ 轉強        turning
   └ 分層        tier + explain + tags
```

三條硬規則：

1. **語意只在後台算一次**，放進 API 回傳的 `explain` 與 `tags` 欄位。
2. **前端所有現算分支全部退化為「印出 `row.explain`」**：`rejectExplain()`、`plain()` 刪掉 `if score<40 / <65` 的字串拼裝；後端 `local_explain()` 收斂到同一份用語表。
3. **改文字 = 只改後台對照表一處**，前端零改動，不可能再牽動篩選。

**不顯示分數(2026-08-07 定案)**：七因子總分（`score_total`）對使用者沒意義、只會跟白話結論打架，一律**不出現在任何說明句或表格欄位**。`score_total` 仍留在 `facts` 供後台內部使用，但前台不顯示、不當語意驅動。連帶：淘汰表移除「七因子總分」欄。

---

## 3. 說明合約（資料結構）

後台每一檔（不論主表 / 觀察 / 淘汰）都附帶下列欄位。前台只讀，不再自算。

```jsonc
{
  "stock_id": "2049",
  "stock_name": "上銀",
  "sector": "無人機",

  // ── 結構化事實（前台各欄位仍可各自使用，但語意不在前台推導）──
  "facts": {
    "change_rate": 8.01,          // 今日漲跌 %
    "flow": 2814,                 // 主動買賣差（張），+流入 / -流出
    "flow_ratio": 0.031,          // 主動淨流入占成交量
    "flow_streak": 3,             // 連續流入(+)/流出(-) 交易日數
    "sector_strong": true,        // 所屬族群是否續強
    "inst_net": 1200,             // 當日三大法人淨買賣（張）
    "inst_3d": 3400,              // 近 3 日法人累計
    "inst_5d": 5100,              // 近 5 日法人累計
    "inst_status": "positive",    // pending | positive | neutral | negative
    "support_state": "hold",      // hold 守 | break 破 | near 逼近
    "turning": true,              // 是否由弱轉強（站上 VWAP/昨高＋相對強度轉正）
    "ma5_distance_pct": 6.2,      // 距 5MA %
    "chase_risk": 82,             // 進場安全分數（越高越不宜追）
    "continuation": 78            // 延續機率分數
  },

  // ── 分層（取代「淘汰制」；只有結構失效才真淘汰）──
  "tier": "STRONG_NO_CHASE",      // CORE | STRONG_NO_CHASE | CANDIDATE | REJECTED
  "tier_label": "強勢觀察｜禁追",

  // ── 白話（唯一顯示來源）──
  "explain": "強勢但乖離高，籌碼尚未確認 → 保留觀察、禁開盤追、等回測昨高或 5MA。",
  "tags": ["族群續強", "主動流入 +2,814", "近 3 日法人連買"]
}
```

`tier` 對照（與 `誤刪率優化` 規格一致）：

| tier | tier_label | 定義 |
|---|---|---|
| `CORE` | 核心觀察 | 趨勢成立、首次突破或健康回測、量價同步、族群強、乖離可接受 |
| `STRONG_NO_CHASE` | 強勢觀察｜禁追 | 今日強、漲幅大、乖離高、籌碼未確認，但趨勢與族群仍強 |
| `CANDIDATE` | 候補觀察 | 結構尚可但資金弱／籌碼中性／量能不足，盤中觸發才升級 |
| `REJECTED` | 淘汰 | **僅**兩項以上結構性失效（跌破支撐 / 假突破 / 量價背離＋族群轉弱） |

> 重要：`chase_risk` 高、`continuation` 低、`inst_status=pending` **都不得**單獨觸發 `REJECTED`。

---

## 4. 白話用語對照表（Canonical Dictionary）

全站唯一一份。位置：後台 `explain.py`。前台與後端舊三套用語全部改引用這裡。

### 4.1 盤中即時（使用者最需要的兩件事）

| 事實條件 | 制式白話 |
|---|---|
| `sector_strong == true` | 「{族群}族群續強（{sector_change:+.1f}%，{N} 檔同步）」 |
| `flow > 0` | 「主動買盤流入 +{flow} 張（佔量 {flow_ratio:.1%}）」 |
| `flow < 0` | 「主動賣壓流出 {flow} 張（佔量 {flow_ratio:.1%}）」 |
| `-threshold < flow_ratio < threshold` | 「資金中性，方向未明」 |
| `change_rate>0 && flow>0` | 「價漲＋主動買盤進場 → 量價同步，買氣真實」 |
| `change_rate>0 && flow<0` | 「拉高卻主動賣超 → 主力邊拉邊出，假紅要防」 |
| `change_rate<0 && flow<0` | 「量價同步走弱 → 資金離場」 |
| `change_rate<0 && flow>0` | 「下跌但有承接 → 逢低買盤進場，觀察止跌」 |

### 4.2 盤後籌碼（能不能挑出潛力股）

| 事實條件 | 制式白話 |
|---|---|
| `inst_net > 0` | 「三大法人淨買 +{inst_net} 張」 |
| `inst_net < 0` | 「三大法人淨賣 {inst_net} 張」 |
| `flow_streak >= 2`（正） | 「近 {flow_streak} 日法人連買」 |
| `flow_streak <= -2`（負） | 「近 {abs} 日法人連賣」 |
| `inst_status == "pending"` | 「盤後籌碼未到 → 待補，不加不扣」 |
| `support_state == "hold"` | 「守住 {5MA/支撐}，支撐未破」 |
| `support_state == "break"` | 「跌破關鍵支撐」 |
| `turning == true` | 「相對大盤由弱轉強」 |

### 4.3 分層結論句（`explain` 主句）

| tier | 制式白話 |
|---|---|
| `CORE` | 「趨勢成立、量價同步 → 可等突破或回測進場。」 |
| `STRONG_NO_CHASE` | 「強勢但乖離高，籌碼尚未確認 → 保留觀察、禁開盤追、等回測昨高或 5MA。」 |
| `CANDIDATE` | 「結構尚可但{弱項} → 盤中觸發後才升級，不主動進場。」 |
| `REJECTED` | 「{結構失效原因1}＋{結構失效原因2} → 結構失效，移出主名單。」 |

> `REJECTED` 的 `explain` **必須**帶出至少兩項具名結構失效原因，不得只寫「分數低」。

---

## 5. 對應現行後台欄位（資料多已存在，只是沒集中）

| 合約欄位 | 現行來源 |
|---|---|
| `flow` / `flow_ratio` | `aflow`（AB 引擎 / `collect.py`）；主動買賣差 |
| `sector_strong` | 族群漲跌 + 族群內續漲檔數 |
| `inst_net` / `inst_3d` / `inst_5d` | `after_hours.py` 三大法人；`collect.py` inst_flow（已加日期護欄 `bc6267c`） |
| `flow_streak` | 法人連買連賣 streak（`collect.py`） |
| `support_state` / `ma5_distance_pct` | 距 `key_support` / MA5 |
| `turning` | 站上 VWAP/昨高 + 相對強度轉正 |
| `chase_risk` / `continuation` | 進場安全分數 / 延續機率分數（雙分數，另立一條線，見 §7） |

---

## 6. 遷移計畫（先量測、後語意，符合逐步檢查習慣）

### 步驟 1：建立說明合約（不碰篩選、不碰版面）
- 新增後台 `explain.py`：把三套現算用語收斂成一份對照表（§4），輸出 `explain` + `tags` + `tier`。
- 定義 API 於每列附帶 `explain`/`tags`/`tier` 欄位。
- **驗收**：API 回傳可見新欄位；舊欄位與版面完全不動。

### 步驟 2：前端改為只印 `row.explain`
- `intraday_decision_dataflow.html`：`rejectExplain()`、`plain()` 刪除 `if score<40 / <65` 分支，改為 `esc2(row.explain)`。
- 後端 `app/ai_explain.py` `local_explain()` 收斂引用同一份用語表。
- **驗收**：全站同一情境只有一種說法；表格欄位、版面、篩選結果不變。

### 非目標（本規格不做）
- 不改任何篩選門檻、不改入選/淘汰的**判定條件**（那是 §7 另一條線）。
- 不改表格欄位、欄位順序、版面。

---

## 7. 與篩選分數改造的關係（獨立一條線）

篩選本身「拆延續機率 / 進場安全雙分數、改分層不淘汰」是**另一件工程**（見 `誤刪率優化` 與 8/07 正式模型定案）。

- 本規格（說明語意層）**先行且獨立**：即使雙分數還沒上線，也能先把「同一篩選不同說法」治好。
- 雙分數上線後，只需把 `chase_risk`/`continuation`/`tier` 餵進說明合約，`explain.py` 對照表不必重寫。
- **關鍵隔離**：改文字永遠不動篩選；改篩選也只更新 `facts`，說明用語表照舊。

---

## 8. 工程驗收標準

1. 全站說明只有一個產生點（`explain.py`），前端零現算。
2. 同一情境（同 facts）在主表 / 觀察 / 淘汰任一處，說法完全一致。
3. `rejectExplain()`、`plain()` 內不再出現 `if score<40 / <65` 這類字串拼裝。
4. 改一句白話只需改 `explain.py` 一處，前端不需改。
5. `inst_status=pending` 顯示「待補」，不顯示為負面。
6. `REJECTED` 的 `explain` 一定帶兩項具名結構失效原因。
7. 表格欄位、版面、篩選結果在步驟 1、2 前後不變（回歸比對）。
