# NEXORA / MLS 三層架構定案（施工唯一契約）

> 2026-07-28 定案。之後所有施工以此為準。核心原則：**盤後資料只餵 Learning，永不回改當日 Decision（防 Data Leakage）**。

## 三層

```
Layer 0  Market Engine（市場環境）  ← regime.py + market.py
         真實寬度 = 上漲家數 / 總家數（TWSE 官方），Risk On / Neutral / Off
         只由「指數 + 真實寬度」決定，永不用 aflow 當市場寬度
         位置：決策首頁頂部（取代舊的 aflow 84.3% 假訊號面板）
              │  Risk Off → 下面兩層一律降/禁倉
              ▼
Layer 1  Intraday Engine（A 鏈）  ← screen_intraday（+ Confirmation 層）
         純即時決策：只用盤中資訊（即時量、五檔、VWAP、分時、大盤同步）
         Signal ≠ Entry：觸發 → 等 3–5 分 → VWAP/EMA5/量縮不跌 確認 → 才進場
         盤後 screen_verify 只調參數/算勝率（Learning 防火牆，不回改今日部位）
         位置：盤後驗證分頁 →「盤中觀測A」按鈕
              │
Layer 2  Swing Engine（B 鏈）  ← b_discover + b_verify + merge_pool
         盤中發現 → 盤後多因子加分（法人 = Confidence Score，非 Trigger）
         隔日 9:10 後重確認（VWAP/量/大盤同步）才進場；09:00–09:10 禁新倉
         位置：盤後驗證分頁 →「盤後驗證B」按鈕
```

## 鐵律（不可違反）

1. **Data Leakage 防火牆**：Decision Engine（Layer 1 即時）與 Learning Engine（盤後 screen_verify）物理隔離。盤後資料只能出「參數/評分」，絕不回改今日已成交或當日訊號邏輯。
2. **Signal ≠ Entry**：任何進場前一定要過 Confirmation 層（延遲確認），不得觸發即買。
3. **法人是加分不是觸發**：B 鏈的法人買超只是 Confidence Score 的一項（+分），不得單獨升格為「法人買超→買」。
4. **禁 Open Buy**：09:00–09:10 一律不開新倉；9:10 後 VWAP/量/大盤同步重確認才進。
5. **A 池 / B 池絕不互通**：A 池（日內）生命週期只有一天，午夜清空、不帶到明天；B 池（波段 3–5 天）每日重新評分排序。被 A 鏈停損者，除非法人佔量異常巨大（>30%），一律禁入 B 池。
6. **Market Regime 是最高層**：Risk Off / Breadth<30% / 加權跌破 20MA / OTC 同步跌 → A 鏈降頻或禁倉；此時 aflow 正值不算承接、法人買超可能只是護盤。

## 檔案 → 層 對照

| 檔案 | 層 | 位置 |
|---|---|---|
| `market.py`（TWSE 真實寬度/指數/類股） | Layer 0 | 首頁 |
| `regime.py`（Risk On/Neutral/Off） | Layer 0 | 首頁 |
| `screen_post.py`（盤後寬篩→候選池） | Layer 1 產池 | 盤中觀測A |
| `screen_intraday.py`（盤中嚴判燈號） | Layer 1 決策 | 盤中觀測A |
| `screen_verify.py`（命中率/勝率） | Layer 1 **Learning** | 盤中觀測A（只顯示準確度） |
| `b_snapshot/b_discover/b_verify/merge_pool.py` | Layer 2 | 盤後驗證B |

## 施工順序

1. **Layer 0**：regime/market 接首頁頂部 + 修 84.3% 假訊號（← 本批開工）
2. B 鏈 `b_verify` 改加分制（法人降為 confidence）
3. A 鏈 Confirmation 層（Signal≠Entry：延遲確認 + VWAP/EMA5/量）
4. 隔日 9:10 重確認 + A/B 池生命週期（A 午夜清空、B 每日重算）

每層可獨立部署、獨立回滾。
