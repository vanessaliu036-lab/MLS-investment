# 盤中行情韌性架構 — 定案規劃

> 目標：把 Shioaji 從「跑在網頁 process 裡的單點」抽離成獨立行情層，讓
> **Shioaji 掛掉只損失 aflow，不再拖垮整個盤中畫面與篩選**。一個月來「每天壞」
> 的病灶是同金鑰多重登入互踢 session + 無隔離無備援無監控；本案逐一根治。
>
> 原則：每階段**收盤後部署**、可獨立驗收、可回滾；不在盤中動網頁 process。

---

## 0. 目標架構

```
Shioaji ─┐                                   ┌─ 8000 server (只讀快照，不登入 Shioaji)
         ├─ Quote Gateway (獨立 daemon) ─快照─┤   · 個股卡片 / intraday_decision UI
TWSE MIS ┘   唯一 Shioaji 擁有者              ├─ feed_bridge → AB 引擎(8002)
             MIS 備援輪詢                     └─ 突破監控 / 量比
             Watchdog + 整包重連
             品質標記(每檔)
```

**不變量（保證不再重演今天的病）**
1. 全系統**只有 Gateway 一個 process 登入 Shioaji**。網頁 process 永不登入。
2. Shioaji 異常 → 價量自動切 MIS 備援；**aflow 標 `UNAVAILABLE`，絕不用五檔壓力偽裝**。
3. 下游把 `aflow=null` 當「未知」，**不可當 0 或流出**（否則製造假訊號，見
   `aflow-sign-shioaji` 記憶）。
4. 每檔對外都帶：`price_source / aflow_source / quote_status / aflow_status / last_tick_age`。

---

## 1. 元件與檔案（已完成、已用盤中真實資料驗證）

| 檔案 | 角色 | 狀態 |
|---|---|---|
| `個股卡片相關檔案_20260722/mis_source.py` | TWSE MIS 免金鑰備援源（tse/otc 自動判別、`z="-"` 用五檔中價補、分批節流） | ✅ 盤中 51/51 全取到 |
| `個股卡片相關檔案_20260722/quote_gateway.py` | 獨立 daemon：Shioaji 唯一擁有者＋MIS 備援＋Watchdog＋整包重連＋原子快照＋HTTP:8005 | ✅ 合併/品質三情境驗證通過 |
| `個股卡片相關檔案_20260722/mls-quote-gateway.service` | systemd unit，`KillMode=control-group` | ✅ 待部署 |

**Watchdog 門檻（quote_gateway.py 常數，可調）**
```
tick 全域無進展 >8s   → DEGRADED
              >20s  → 整包重連(dispose→login→subscribe→驗第一筆tick)
連續 3 次重連失敗      → 價量切 MIS 備援(BACKUP)
aflow >15s 未更新     → aflow_status = UNAVAILABLE
```

**快照契約（消費端只依賴這個；日後換 Redis 免動消費端）**
- 路徑：`/dev/shm/mls_quote_gateway.json`（tmpfs，原子替換）
- 或 HTTP：`GET http://127.0.0.1:8005/quotes`、`/health`
```json
{ "ok": true, "feed_state": "LIVE|DEGRADED|RECONNECTING|BACKUP|CLOSED",
  "last_tick_age": 1.4, "counts": {"quote_live":51,"quote_backup":0,"aflow_live":51},
  "rows": [ { "code":"2049","price":338.5,"change_rate":2.0,"total_volume":1600,
              "aflow":300,"buy_volume":900,"sell_volume":600,
              "price_source":"shioaji","aflow_source":"shioaji",
              "quote_status":"LIVE","aflow_status":"LIVE","last_tick_age":1.4 } ] }
```

---

## 2. 分階段落地（每段收盤後獨立部署）

### Phase 0 — 清潔止血（最低風險，可最先做）
與行情層無耦合，先把「重啟反而更糟」與雜訊 bug 根治。
- **修 systemd 殺不乾淨**：`mls-intraday.service` 加 `KillMode=control-group`
  （或 `mixed`）＋ `ExecStartPre=/usr/bin/pkill -f "server:app"` 保底，讓重啟
  一定收乾淨 → 不再殘留多代 Shioaji 登入。
- **除 `gatekeeper` 幽靈模組**：`engine.py:464` 的 `import gatekeeper` 每個排程
  週期都 `ModuleNotFoundError`。確認該功能是否還需要 → 補模組或移除 import。
- 驗收：`ps -eo pid,etimes,cmd | grep server:app` 只剩一代；journal 無 gatekeeper traceback。
- 回滾：還原 unit 檔即可。

### Phase 1 — 單一登入 + 行程內 MIS 備援（安全落地版）★ 【本次已實作】
**發現**：雙重登入的結構根源是 `server.py` 用 `multiprocessing.Process` 另開排程
子行程，該子行程對同一金鑰再登入一次 Shioaji → 與 HTTP worker 互踢 session。
故最穩的根治不是把整個卡片引擎搬去 gateway（daily_kbars/index 遍布 6 支檔、盲改
高風險），而是「**讓全服務只有一個 Shioaji 登入**」＋「**行程內** MIS 備援」。

已完成改動（新增/修改，皆通過 py_compile；備援用盤中真實資料驗證）：
- `server.py`：排程 `multiprocessing.Process` → `threading.Thread`（共用唯一登入）。
- `個股卡片相關檔案_20260722/quote_health.py`【新】：行程內健康判定 + MIS 備援疊加。
  不登入、不另開行程；吃 broker buffer，死 → MIS 蓋價量、aflow 標 UNAVAILABLE。
- `vps_intraday_test.py`：`intraday_test()` 套 `quote_health.apply()`；`_row()` 依
  `_aflow_unavailable` 停用 aflow、輸出 `price_source/quote_status/aflow_status/last_tick_age`；
  回傳新增 `feed_health`。
- `mis_source.py`【新】：TWSE MIS 備援源（已 51/51 驗證）。
- `engine.py`：`gatekeeper` 缺模組時優雅略過（Phase 0-B）。
- `mls-intraday.service`：`ExecStartPre pkill` + `KillMode=mixed`（Phase 0-A）。

驗收（隔日開盤）：
- `ps -eo pid,etimes,cmd | grep server:app` → **單一 process**（無 forkserver 子行程）。
- `journalctl -u mls-intraday | grep SessionNotEstablished` → 開盤後無此錯。
- `curl :8000/api/intraday-test` → `feed_health.feed_state=LIVE`、51 檔 vol>0、aflow!=0。
- 模擬掛掉：盤中 `iptables` 擋 Shioaji 或觀察真實斷線 → 20s 後 `feed_state=BACKUP`、
  價量仍在（price_source=twse_mis）、`aflow_status=UNAVAILABLE`（不歸零、不偽裝）。
- 回滾：`git checkout` 這批檔 + `systemctl restart mls-intraday` 即回舊行為。

### Phase 2 — 資料品質可視化 + 告警
- `intraday_decision.html`：每檔加徽章 `LIVE / BACKUP / AFLOW停用`；頂部一條 feed 健康列
  （feed_state + last_tick_age + live/backup 檔數）。BACKUP 時明示「aflow 暫停、僅價量」。
- （選配）feed_state 非 LIVE 連續 >N 秒 → PushNotification，不必等畫面全空才發現。
- 驗收：拔掉 gateway 時 UI 立刻顯示 BACKUP 徽章、aflow 欄顯示「停用」而非 0。

### Phase 3 — 消費端統一 + 移除安全網
- `feed_bridge.py`：現讀 8000（Phase 1 後已反映 gateway 資料，可不動）；若要更乾淨，
  直接改讀 `:8005/quotes`，與 8000 完全解耦。
- 移除 Phase 1 的 broker fallback 分支（確認 gateway 穩定運行 ≥1 週後）。
- AB 引擎的盤中嚴判：確認 aflow=null 時走「待資料」分支，不誤判流出。

### Phase 4 — 選配強化（穩定後再談）
- **Redis 換掉 /dev/shm 檔**：契約不變，`snapshot()` 改寫 Redis、消費端改讀 Redis。
- **指數(TAIEX)源韌性**：加權指數快照也納入 gateway（含 IX0001 容錯，broker 已修）。
- **突破監控**：以 gateway 的價量做觸發價監控，Shioaji 掛也照跑。

---

## 3. 為什麼這樣就「實用順暢、不再每天壞」

| 舊病 | 根治手段 |
|---|---|
| Shioaji 在網頁 process 內、單點 | Gateway 獨立 daemon，網頁只讀快照 |
| 多重登入互踢 session | 全系統單一登入者不變量 + KillMode 收乾淨 |
| 只 re-login 不重建 → 反覆斷 | §4 整包重連：dispose→login→subscribe→驗 tick |
| Shioaji 一掛整個畫面死 | MIS 備援：價量照跑，只損失 aflow |
| 拿五檔假裝 aflow → 假訊號 | aflow 只信 Shioaji，缺就標 UNAVAILABLE |
| 壞了才發現 | Watchdog 狀態機 + 品質徽章 + 告警 |
| 重啟殘留、幽靈模組 | Phase 0 清潔 |

---

## 4. 收盤後部署清單（Phase 0 + Phase 1，本次已寫好）

改動檔（皆已 py_compile 通過；MIS/備援已用盤中真實資料驗證）：
```
mls-intraday.service                         # Phase 0-A KillMode + ExecStartPre
個股卡片相關檔案_20260722/engine.py           # Phase 0-B gatekeeper 優雅略過
個股卡片相關檔案_20260722/server.py           # Phase 1  Process→Thread(單一登入)
個股卡片相關檔案_20260722/mis_source.py        # 新 · MIS 備援源
個股卡片相關檔案_20260722/quote_health.py      # 新 · 行程內健康判定+MIS疊加
vps_intraday_test.py                          # Phase 1 接 quote_health + 品質欄位
```

部署步驟（收盤後、13:30 之後）：
1. `git` 提交這批 → `./deploy_vps.sh`（或 rsync 這幾支 + service）。
2. 更新 unit：`scp mls-intraday.service root@VPS:/etc/systemd/system/ && systemctl daemon-reload`。
3. `systemctl restart mls-intraday`。
4. 驗收：`ps ... | grep server:app` 單一 process；`curl :8000/api/intraday-test`
   看 `feed_health`。隔日開盤看 aflow!=0、無 SessionNotEstablished。
回滾：`git checkout` 這批檔 + 還原 unit + `systemctl restart`。

## 5. 之後（穩定後再談，非本次）

- **Phase 2 UI**：`intraday_decision.html` 依 `feed_health` / 每檔 `quote_status`
  加徽章（LIVE／MIS備援／aflow停用）＋頂部健康列；BACKUP 時明示「僅價量」。
- **Phase 3 完全隔離**：把 Shioaji 抽成獨立 daemon（已備妥 `quote_gateway.py` +
  `mls-quote-gateway.service` 骨架），網頁只讀快照。等 Phase 1 穩定運行 ≥1～2 週、
  且有空檔（非交易時段）做完整 de-Shioaji（daily_kbars/index 改走 gateway）再切。
- **Phase 4 選配**：Redis 換 /dev/shm、指數源韌性、突破監控走備援。
