# 盤後晚補跑:14:40/15:05 時 FinMind 晚出的個股 EOD(常是上櫃/TPEx)還沒到,
# 導致 daily_bar/inst_flow 缺約 21 檔 → 盤後驗證在殘缺樣本上失真、明日 pool 缺籌碼。
# 17:30 再跑一次 run_stage2.sh(collect INSERT OR IGNORE 只補缺 + 重驗 + 重建乾淨 pool)。
# 加此 OnCalendar 為疊加(保留原 15:05),不覆蓋。
[Timer]
OnCalendar=Mon..Fri 17:30:00 Asia/Taipei
