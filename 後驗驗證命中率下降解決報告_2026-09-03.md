# 後驗驗證命中率下降解決報告

日期：2026-09-03  
範圍：正式站 `mls.db`、EOD QA、T+1 盯盤名單驗證、後驗 API/UI 與選股控制器

## 一、執行摘要

目前看到的「命中率越來越低」，不能直接判定為模型能力持續衰退。正式站同時存在兩套驗證口徑，以及一條未被 QA 阻斷的資料管線，導致顯示值、訓練回饋與模型控制器互相污染。

最嚴重的問題是：新版 v4.1 已寫入 `A_突破成功`、`B_續強`、`C_未續強`、`突破延續` 等結果，但舊的 `/api/watch-history` 仍只計算 `兌現`、`反向`，因此正式 API 顯示 `0/218 = 0.0%`。同一批正式資料依新版口徑重算，實際為 `45/218 = 20.6%`；最近 5 個有有效名單的交易日為 `12/50 = 24.0%`，目前沒有證據支持「命中率單調下滑」。

真正需要先修的是資料與指標可信度，再判斷是否需要調整模型。

## 二、關鍵證據

| 項目 | 目前結果 | 判讀 |
|---|---:|---|
| 舊 `/api/watch-history` | 0/218 = 0.0% | 結果字串已過時，屬顯示/API 錯誤 |
| 新 `/api/review`，2026-09-01 | 4/10 = 40.0% | 同日新版 v4.1 的正確摘要 |
| v4.1 全部有效名單 | 45/218 = 20.6% | 目前可採用的初步總體數字 |
| 最近 5 個有效交易日 | 12/50 = 24.0% | 未呈現單調下降 |
| 最近 10 個有效交易日 | 21/100 = 21.0% | 與前 10 個有效日 22.4% 接近，屬輕微波動 |
| `review_log` 總日數 | 33 日，其中 10 日空名單 | 空名單被寫成 0.0%，污染趨勢 |
| 含空日的日均命中率 | 13.8% | 不應拿來與有效日均值比較 |
| 排除空日後的日均命中率 | 19.9% | 較接近真實表現 |
| EOD QA | 28 次僅 1 次通過 | 驗證與學習回饋長期不穩定 |
| `signal_outcomes` | 42 筆，集中在單一交易日 | 不足以驅動自動調參 |

補充：在另一個較寬鬆的「收盤漲幅超過訊號價 +0.3%」標籤下，選中樣本為 84/198 = 42.4%，全 51 檔基準為 494/1275 = 38.7%。這表示選股可能仍有些微篩選效果，但它不是 v4.1 的「+2% 且相對產業強勢」命中率，不能混用。

## 三、根因分析

### P0：驗證結果字串不一致，API 直接算成零命中

`after_hours.py` 的 v4.1 驗證已使用新版 verdict；`server.py` 的 `/api/watch-history` 仍硬編碼查找 `兌現` 和 `反向`。由於正式資料中沒有 `兌現`，舊 API 必然回傳零命中。舊 UI 仍呼叫這個 endpoint，因此使用者會看到錯誤的下降曲線。

相關位置：

- `個股卡片相關檔案_20260722/server.py:2194-2227`
- `個股卡片相關檔案_20260722/after_hours.py:150-195`

### P0：空名單的 0/0 被儲存成 0.0%

`db.write_review()` 在總樣本數為零時仍寫入 `hit_rate=0.0`。這使得「沒有名單、沒有驗證」被誤當成「有名單但全數失敗」，進而拉低日均和趨勢圖。

相關位置：

- `個股卡片相關檔案_20260722/db.py:420-439`
- `個股卡片相關檔案_20260722/db.py:469-475`

### P0：EOD QA 只記錄問題，沒有阻止錯誤資料進入後續流程

QA 已經能檢查 snapshot coverage、sector_daily、當日 signals、signal_outcomes 和 NULL labels，但 QA 失敗後流程仍會繼續回填標籤、寫入 features、匯出資料、記錄摘要與發送報告。這讓「不完整日」有機會成為訓練或控制器的輸入。

正式站 28 次 QA 僅 1 次通過；常見錯誤包括 sector_daily 缺失、有進場訊號卻沒有 signal_outcomes，以及標籤未完成。

相關位置：

- `個股卡片相關檔案_20260722/eod_pipeline.py:107-156`
- `個股卡片相關檔案_20260722/eod_pipeline.py:245-286`
- `個股卡片相關檔案_20260722/after_hours.py:545-565`

### P1：控制器的目標與 v4.1 命中定義不一致

v4.1 的 headline 命中條件是突破成功、收盤確認、相對產業為正，且報酬至少約 +2%；但 factor feedback 與 precision controller 使用的是 `收盤價 > 訊號價 * 1.003`，即 +0.3% 目標。控制器因此可能在優化另一個指標，卻改變 v4.1 的選股門檻。

目前還有 `rolling_precision(days=10)` 在有效樣本不足、目標未統一的情況下調整 `entry_score_min`。這會把噪聲放大成門檻漂移。

相關位置：

- `個股卡片相關檔案_20260722/after_hours.py:612-654`
- `個股卡片相關檔案_20260722/config.py:11-17`

### P1：標籤回填只處理一個最新日期，可能造成資料積壓

`backfill_labels()` 只選一個最新的 unresolved `trade_date`，而且缺少股票快照時只會留下 NULL。結果是 training_samples 可能長期有未標記日期，QA 會持續失敗，但流程仍照常往下走。

相關位置：

- `個股卡片相關檔案_20260722/eod_pipeline.py:195-216`

### P1：為湊滿 10 檔而補入低品質名單

`select_radar_watchlist()` 會把 `可操作` 和 `觀察` 填到最多 10 檔，沒有最低分數、資料完整度或流動性門檻。正式資料中 `觀察` 命中率 24.3%，反而高於 `可操作` 的 17.4%，顯示目前分類/分數與 T+1 目標未校準；不應直接用提高門檻的方式修正。

## 四、解決方案與優先級

### P0：先統一驗證語意與 API

1. 建立單一 canonical `is_hit(verdict, model_version, metric)`，至少明確區分：
   - `v4.1_A`：`A_突破成功` + `突破延續`。
   - `v4.1_B`：A 加上 `B_續強`，作為較寬鬆觀察指標。
   - `legacy_0.3pct`：舊的收盤漲幅 +0.3% 指標。
2. `/api/review`、`/api/watch-history`、所有 UI 趨勢圖都使用同一個 canonical 計算結果。
3. 保留舊 verdict 文字的相容讀取，但禁止 endpoint 各自硬編碼命中條件。
4. 回傳 `metric`、`model_version`、`denominator`、`valid_days`，避免只顯示一個百分比。

### P0：把「沒有資料」與「命中率為零」分開

1. `watch_total=0` 時，`hit_rate` 應為 NULL/空值，並標記 `DATA_INCOMPLETE` 或 `NO_WATCHLIST`。
2. 趨勢統計只納入 `watch_total > 0` 且驗證完成的日期。
3. UI 顯示 `—` 或「資料不足」，不要顯示 0.0%。
4. 對每個交易日建立 expected / observed / missing 的對帳摘要。

### P1：讓 EOD QA 成為硬閘門

QA 失敗時可以保存原始資料與失敗紀錄，但必須停止：

- factor weight 更新；
- `entry_score_min` 自動調整；
- 新訓練資料發布；
- 對外宣稱該日驗證完成。

最低放行條件建議為：snapshot coverage ≥95%、sector_daily 完整、若有 buy signal 則 signal_outcomes 已產生、待標記樣本為零。未通過的日期只能標記 `PENDING_DATA`，不可進入模型回饋。

### P1：修正標籤回填與狀態機

按交易日由舊到新回填所有 unresolved dates；每筆樣本明確使用 `VERIFIED`、`PENDING_DATA`、`NO_DATA`、`INVALID` 狀態。回填數量、缺少的股票代號與最後可重試時間要寫入 QA log，不可靜默略過。

### P1：停用錯誤目標驅動的自動調參

在 `legacy_0.3pct` 與 `v4.1_A` 尚未拆開、且 signal_outcomes 未達最低樣本前，暫停 `rolling_precision` 對 `entry_score_min` 的自動調整。後續若恢復，只能以與正式決策完全一致的目標、樣本窗口和版本進行調整。

### P2：用乾淨前向資料再判斷模型是否衰退

現有 F4 已標記為 research-valid、production gate failed；不要拿被污染的 2026 holdout 反覆重調。完成 P0/P1 後，固定模型與門檻，累積至少 20 個「QA 通過且驗證完成」交易日，再比較：

- `v4.1_A` 命中率與平均報酬；
- `v4.1_B` 命中率與回撤；
- 全 51 檔基準；
- 隨機/等權基準；
- 可操作與觀察兩個分組。

同時停止無條件補滿 10 檔；若沒有通過品質門檻，應允許當日少於 10 檔或空名單，並明確標示原因。

## 五、驗收標準

修復完成後必須全部符合：

1. `/api/review` 與 `/api/watch-history` 對同一版本、同一日期回傳完全一致的命中數與分母。
2. 空名單不再被計為 0.0%，趨勢能區分 `NO_WATCHLIST`、`PENDING_DATA` 與 `VERIFIED`。
3. 每個有效交易日可從 watchlist → watch_outcome → summary 逐筆對帳。
4. 連續 5 個交易日 EOD QA 通過後，才允許恢復任何自動調參。
5. Snapshot coverage ≥95%，有 buy signal 時 signal_outcomes 不可為零，訓練樣本不可有未處理 NULL。
6. 報表固定同時呈現 metric、model_version、樣本數、有效日數、平均報酬，不再只呈現單一 hit rate。
7. 累積至少 20 個乾淨前向交易日後，才決定是否修改模型；在此之前只修管線與觀測層。

## 六、結論與執行順序

目前最可信的初步數字是：v4.1_A 為 `45/218 = 20.6%`，最近 5 個有效交易日為 `12/50 = 24.0%`。`0/218 = 0.0%` 是舊 API 對新版 verdict 的錯算；被空名單拉低的日均也不是模型命中率。

建議執行順序：

1. 修 canonical verdict 與兩個 API/UI 的一致性。
2. 排除 0/0，補上資料狀態與分母。
3. 將 EOD QA 改成硬閘門。
4. 修正全量標籤回填。
5. 暫停錯誤目標驅動的自動調參。
6. 固定模型收集乾淨前向資料，再做是否換模/調門檻的決策。

本報告只完成診斷與解決方案，未修改正式站、遠端資料庫或模型程式。
