# 交接 — 2026-08-23

## 一句話現況
Pre-Activation v0.1（規則版）後端已上線並接進排程，**8/24 盤後開始收 live 快照**。
前端 badge 已接進兩條路徑，但**觀察池那頁還沒接**，且有一個時間戳沒修完。

---

## 一、正在等的事（不需要人介入）

**8/24（週一）盤後**會自動發生：
1. `run_stage2.sh` 依序跑 `collect.py` → `run_stage2_verify.py` → **`run_pa_snapshot.py`**
2. `run_pa_snapshot.py` 呼叫 `screen_post.build()`，寫 51 檔 stage 進 `pa_snapshot` 表
3. 隔日起自動回填 `T+1/T+3/T+5/T+7` 的 gross、扣 47.1bps 的 net、MFE/MAE
4. 讀出用 `pa_report.summary_text()`（樣本 < 20 筆只列出、不下結論）

**這批 8/24 之後的快照是唯一乾淨的驗證集**——2026 holdout 已在研究端被我看過（污染紀錄在
`~/Desktop/winning_model_backtest/FROZEN_F4.md`）。不要用舊歷史去調 Pre-Activation 規則。

---

## 二、沒做完的（照優先序）

### 1. ⚠ 觀察池頁的資料時間戳卡在「讀取中…」
- 頁面：`/watch-first-layer` → 載入 `個股第一層ＵＩ.html`（**不是** `intraday_decision_dataflow.html`）
- 原本是寫死的 `2026-07-23T15:05:13`，看起來像資料過期一個月，其實 `/api/watchpool` 是新的
- 已改成 `<div class="time" id="poolStamp">`，並在兩個 fetch 點加了更新碼
- **但實測仍顯示「資料時間讀取中…」** → `syncLiveFields()` 沒跑到，或被 `if(busy)return` 擋掉，
  或只掛在某個尚未觸發的 timer。下一步：查 `syncLiveFields` 的呼叫時機
- 已部署，不影響其他功能（只是時間戳沒更新）

### 2. 觀察池頁看不到 Stage badge
- badge 只接在 `intraday_decision_dataflow.html` 的兩處：`renderTomorrowGroups`、明日觀察清單表
- 觀察池走的是**另一個檔案** `個股第一層ＵＩ.html`，用**另一個 API** `/api/watchpool`
- 我只把 `pre_activation` 併進 `/api/intraday-watchpool`（已驗證 51/51），
  **`/api/watchpool` 還沒併**
- 要做：`vps_intraday_test.py` 的 `_attach_pre_activation(rows)` 也套到 `/api/watchpool`，
  再改 `個股第一層ＵＩ.html` 顯示 badge

### 3. 決策首頁的「盤中即時行情與分類」表也沒有 badge
- 使用者最常看的表（欄位：股票／族群／aflow／漲跌／分類／白話解讀）
- 刻意沒動，怕再弄壞首頁。要加的話**務必先在本機起 server 用瀏覽器截圖驗證**

---

## 三、血的教訓（務必遵守）

1. **改前端一定要實際渲染驗證，不能只做語法檢查。**
   2026-08-22 我把 helper 插在既有函式之間，`new Function()` 21/21 通過，
   但實際載入後版面整個垮掉，被迫回滾。
   正確做法：helper 包成獨立 IIFE 掛 `window`、放在 `</body>` 前、不碰既有函式；
   呼叫端寫 `${(window.paBadge?paBadge(x):'')}`；部署前 `python3 -m http.server` + 瀏覽器截圖。

2. **「只進不退」要套在每一個寫入點，不是只有 DB 表。**
   `intraday_live_snapshot.json` 漏了守門 → 非交易日寫入 aflow 全空的快照，
   把前一交易日有值的洗掉，畫面 51 檔 aflow 全變「—」。
   資料其實一直在 `intraday_eod.db` 的 `intraday_stock_daily`。
   **看到欄位變空，先分辨「沒抓到」還是「被空值覆蓋」——修法完全不同。**

3. **同一份資料不要在兩個地方各算一次。**
   盤中那支不重算 stage，一律從 AB 引擎 `candidate_pool.payload` 唯讀併入。

4. **前端有多份 HTML，改之前先確認使用者看的是哪一份。**
   `intraday_decision_dataflow.html`（首頁）、`個股第一層ＵＩ.html`（觀察池）是兩份不同檔案。

---

## 四、關鍵檔案

| 檔案 | 用途 |
|---|---|
| `篩選邏輯/pre_activation.py` | 四階段規則判定（EARLY/ARMED/TRIGGER/EXTENDED） |
| `篩選邏輯/pa_snapshot.py` | 每日快照 + T+1/3/5/7 回填（進場價一律 T+1 開盤） |
| `篩選邏輯/pa_report.py` | 讀出，n<20 不下結論 |
| `篩選邏輯/run_pa_snapshot.py` | 排程入口（掛在 `deploy/run_stage2.sh` 最後一行） |
| `intraday_decision_dataflow.html` | 決策首頁（badge 已接） |
| `個股第一層ＵＩ.html` | 觀察池頁（badge **未**接、時間戳待修） |
| `vps_intraday_test.py` | 8000 服務：快照守門、`_attach_pre_activation` |

## 五、線上環境

```
8000  mls-intraday       /opt/mls-intraday                前台 + 個股卡片
8002  mls-ab-engine      /opt/mls-screen                  AB 引擎（正本）
排程  mls-screen-stage2.timer → deploy/run_stage2.sh
測試  篩選邏輯/ 155 綠 · winning_model_backtest 36 綠
```

## 六、研究結論（已凍結，不要再優化）

`FROZEN_F4.md` / `FROZEN_HYPOTHESES.md` / `PREREGISTERED_B2.md` 在
`~/Desktop/winning_model_backtest/`。摘要：

- F4 ranking model：**Research-valid / Production gate failed**（排序有 edge，扣成本後不賺錢）
- 三段事件鏈（外資→量能→融資）：融資段未 replicate，**FAIL**
- 兩段事件順序：discovery 不支持，**FAIL**
- 現行 51 檔模型定位：**「在這 51 檔內、跨時間驗證過的專用篩選模型」**，不是通用因子
